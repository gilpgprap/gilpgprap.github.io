// Crea la variable a y le asigna 0.
let a = 0;

let b = 0; // Crea la variable b y le asigna 0.

/* Crea la variable c y le asigna 0. */
let c = 0;

/* Crea la variable d y
le asigna el valor numérico 0. */
let d = 0;

/** Variable numérica cuyo valor inicial es 0.
 * @type {number} */
let e = 0; 